#include "main.h"
/**
 * _abs - gets the abosoulute value of an integer
 * @n: the int to check
 * return: the abs of the integer
 */
int _abs(int)
{
	if (n>0)
	{
		return n;
	}
	else 
		return -n;
}
